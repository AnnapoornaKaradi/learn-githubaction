name: my first workflow

on: push

jobs:   
     first_job:
         runs_on: ubuntu-latest
         steps:
         - name: welcome message
           run: echo "my first github action"
     
         - name: list files
           run: ls 

         - name: read files
           run: cat readme.md
           